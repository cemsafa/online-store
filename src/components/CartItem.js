import React from "react";
import { View, Text, StyleSheet } from "react-native";

const CartItem = () => {
  return (
    <View style={styles.viewStyle}>
      <Text style={styles.textStyle}>CartItem</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  viewStyle: {},
  textStyle: {},
});

export default CartItem;
