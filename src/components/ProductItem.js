import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ProductItem = () => {
  return (
    <View style={styles.viewStyle}>
      <Text style={styles.textStyle}>ProductItem</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  viewStyle: {},
  textStyle: {},
});

export default ProductItem;
