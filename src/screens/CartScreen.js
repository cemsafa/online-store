import React from "react";
import { View, Text, StyleSheet } from "react-native";

const CartScreen = () => {
  return (
    <View style={styles.viewStyle}>
      <Text style={styles.textStyle}>CartScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  viewStyle: {},
  textStyle: {},
});

export default CartScreen;
